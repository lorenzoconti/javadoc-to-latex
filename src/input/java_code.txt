/**
* Returns an Image object that 2*2 can then be painted on the screen.
* @version 3.8
 * @author Lorenzo Conti, Paolo Fosci, Giuseppe Psaila
*/
public Image getImage(URL url, String name) {
    /**
     * @param parametro Lorenzo Conti, Paolo Fosci, Giuseppe Psaila
    */
    return getImage(new URL(url, "/**@author name"));
    PRIMO
  }
}
/** Returns an Image object that 2*2 can then be painted {@code prova con code} on the screen. */
public Image getImage(URL url, String name) {
    return getImage(new URL(url, "name"));
    SECONDO
}
/** inline */
    /** inline */


        /**
        * inline
        *
        */